Tela {
        background: #111;
        overflow: hidden;
    }

#fundo_mapa {
    text-style: bold;
    color: #555;
}

/* Garante que as entidades flutuem por cima do mapa */
.person, .enemy {
    position: absolute;
    width: 2;
    height: 1;
}